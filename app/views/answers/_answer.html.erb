<article id="<%= dom_id(answer) %>" class="answer">
    <p class="about">
      <%= render "shared/record_meta", record: answer %>
    </p>

    <%= simple_format strip_tags(answer.content) %>
</article>
